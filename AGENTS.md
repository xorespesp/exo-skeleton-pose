# exo-skeleton-pose

트레드밀을 걷는 로봇의 하지 관절에 마커(현재 RGB AprilTag, 향후 IR/depth 가능)를 부착해 관절 각도를
추정하고, 그 포즈를 네트워크로 외부 클라이언트에 노출하는 프로젝트.

이 문서는 **큰 그림과 설계 근거**만 담는다. 필드명·키 경로·UI 라벨·수치처럼 리팩토링이 바꾸는 것은
코드가 유일한 출처다. 여기 적을 값어치가 있는 것은 **바꾸려면 설계를 다시 해야 하는 것들**이다.

---

## 아키텍처

계층별로 디렉터리가 나뉜다. 각 계층에서 볼 것은 **책임**과 **바깥이 모르게 감춘 seam** 이다.

### `src/hw/` — 카메라 입력

SDK 비종속 추상 인터페이스, 백그라운드 스레드로 프레임을 pull 해 observer 에게 push 하는 스트리밍
provider, 교체 가능한 백엔드로 구성된다. 백엔드는 `src/hw/backends/` 에 모여 있고 현재 둘이다:
Orbbec K4A Wrapper 기반과 Vieworks VZ(Galaxy C++ API) 기반.

**백엔드를 아는 곳은 provider 구현부와 그 디렉터리뿐이다.** 카메라나 SDK 를 추가·교체할 때 그 두
자리만 바꾸면 상위 계층은 불변이다. K4A 는 SDK 가 intrinsic 을 주지만 VZ 는 주지 않아, 오프라인
캘리브레이션 파일을 config 로 받아 넣는다.

프레임 기하(ROI, 그에 맞춰 재타겟된 캘리브레이션)는 **한 함수만 쓴다.** 열 때와 ROI 를 걸 때 둘 다
그곳을 거치므로 픽셀 프레임이 통지 없이 움직일 수 없다. 폴링 스레드가 그 통지를 프레임 사이에서
내보내고, **픽셀 좌표에 매달린 상태를 들고 있는 쪽이 스스로 버린다.**

### `src/pose/` — 검출과 추정

마커 검출로 관절 위치를 얻고, 그 위치로부터 IK 로 관절 회전(부모 기준, 캡처한 rest 대비)을 구한다.
관절과 태그의 매핑·부모 관계는 코드에 흩지 않고 **데이터 주도 테이블 하나**로 관리한다(관절 추가 =
테이블 한 줄).

**관절은 분절점이다.** 한 관절의 상태(회전·각도)는 그 관절에서의 굽힘이고, 자기에게서 자식으로
나가는 뼈를 돌린다(hip 이 허벅지를, knee 가 정강이를). 태그 하나를 여러 관절이 공유할 수 있어서
골반 마커에는 pelvis 루트와 양쪽 hip 이 겹쳐 있고, 그 태그의 검출 하나가 셋 모두의 측정이 된다.
발은 체인을 닫는 마커 end site 라 위치만 갖고 각도·회전은 항상 비어 있다.

**마커 기술마다 다른 것은 전부 `marker_tracker_base` 뒤에 있다**: 어떤 프레임이 필요한지, 어떻게
검출·표시하는지, 검출을 어떻게 관절 이름까지 끌고 가는지, rest pose 보정 때 할 일이 있는지. 구현이
둘이다.

- `apriltag_tracker` — id 가 관절을 바로 주므로 표 조회
- `color_marker_tracker` — 단색 원반이라 놓인 순서로 이름을 붙인다 (sagittal 전용)

추정기는 그 산물인 측정치만 받아서 **마커 기술을 모른다.** 트래커를 추가할 때 파이프라인이 분기하는
곳은 소스를 여는 한 군데뿐이고, 종류를 알아야 하는 GUI 는 `dynamic_cast` 로 묻는다.

추정기도 마찬가지로 `pose_estimator_base` 뒤에 있다. frontal 은 3D 위치에서 IK 로, sagittal 은 이미지
평면의 2D 점에서 각도로 푼다. **읽는 쪽(플롯, 트레이스, 프로토콜)은 어느 쪽이 도는지 모른다.**

### `src/net/` + `proto/` — 프로토콜

`proto/exo_pose_proto.fbs`(FlatBuffers)가 유일 소스이며, uWebSockets 서버가 이를 통해 포즈 스트림
start/stop, rest pose 보정, 관절 포즈 스트리밍을 노출한다.

**무엇을 여는지는 서버 config 소유라 커맨드에 인자가 없다.** 검출·추정을 담당하는 포즈 파이프라인은
네트워크 비종속 코어로 분리돼 있고, 서버는 그 위의 트랜스포트·브로드캐스트 계층이다. 클라이언트는
언어 중립(현재 three.js 데모, 실제 운영은 C# 예정).

**카메라 영상은 무거워서 프로토콜에 싣지 않는다.**

### `src/gui/` — 디버그 GUI

imgui + SDL3 기반. 서버를 소유해 리스너를 start/stop 하고, 소스 open/close 와 rest pose 보정을 서버와
공유하는 포즈 파이프라인으로 수행하며, 관절 위치·스켈레톤·어노테이트된 영상을 확인한다.

### `webview/` — 데모 클라이언트

three.js(Vite). 위 프로토콜로 받은 포즈로 리깅된 glTF 캐릭터를 구동한다. **프로토콜 동작을 검증하는
테스트 하네스일 뿐 제품이 아니다.**

### 데이터 흐름

```
백엔드 → provider → observer → 트래커 → 추정기 → (GUI 표시 + 서버 broadcast)
```

GUI 와 서버는 이 파이프라인 하나를 공유하므로, 소스나 보정을 어느 쪽에서 조작해도 상태가 어긋나지
않는다.

---

## 좌표계 규약

리그 공간은 **장비에 붙은 프레임**이다. 카메라가 어디 서든 안 움직인다.

```
+X  장비의 왼쪽      +Y  아래      +Z  장비의 뒤
```

모든 다리 관절은 **+X 한 축**에 대한 굴곡이고, `local_anim_rot` 은 그 축에 대한 우수 부호다. 이 축은
config 값이 아니라 상수 하나이며, 회전을 만드는 쪽과 각도를 역산하는 쪽이 그 심볼을 공유한다. 그래서
frontal / sagittal 좌 / sagittal 우가 **구조적으로 같은 규약**을 낸다.

뷰마다 다른 것은 그 축이 **카메라 좌표에서 어디에 놓이는지**뿐이다.

| 뷰 | 카메라 좌표에서의 힌지 축 |
|---|---|
| frontal | 카메라 `+X` (두 프레임이 일치) |
| sagittal, 장비 왼쪽에서 | 카메라 `-Z` (광축) |
| sagittal, 장비 오른쪽에서 | 카메라 `+Z` |

sagittal 은 측정치를 리그 프레임으로 **스스로 옮긴다.** 어느 쪽에서 찍었는지는 마커를 붙인 다리에서
따라오고, 그 부호가 위치 변환과 각도 변환에 같이 걸려 상쇄된다.

frontal 은 변환을 하지 않고 **카메라 공간을 리그 공간으로 그대로 쓴다.** 그래서 정면·수평 설치가
요구사항이다. 롤에 대한 보정 수단은 코드에 없다(오차는 2차항이라 눈대중 수평이면 충분하다).

`local_anim_rot` 과 각도들이 함께 와이어에 실리지만 **역할이 갈린다**: 리그 구동은 회전으로만 하고,
각도는 측정값으로 읽는다. 1-DOF 힌지 제약 없이 푼 회전은 축 외 성분을 갖고 각도는 그것을 못 보므로,
둘이 항상 같은 포즈를 말하지는 않는다.

각도의 기하 계산은 **리그 힌지 부호(후방 +) 하나로만** 하고, `joint_state_t` 에 담기는 경계에서
`docs/joint_angle_convention.md` 의 생체역학 표준 표현(Segment Angle 전방 +, Clinical Joint Angle
굴곡 +)으로 한 번 변환한다. 관절별 부호와 발목 중립 오프셋은 `joints_def.hh` 의 테이블 한 곳에만
있다. 측정 각도는 rest 캡처 없이 흐르고 인코더 검증과 직접 비교되며, rest 는 `local_anim_rot` 과
rest 대비 delta 각도만 게이트한다. 와이어에는 sagittal 접두의 segment / clinical / included 셋이
실린다. included 는 추정기가 clinical 을 채우는 그 자리에서 같은 공식으로 함께 채우므로 둘이 어긋날
수 없고, 서버·트레이스·플롯은 `joint_state_t` 를 읽기만 한다. Segment angle 은
리그 lateral 축에 대한 마운트 기울기(사이드 뷰에서는 카메라 롤)가 1차로 들어가는 값이라 수평 설치
요구가 그대로 적용되고, clinical 은 인접 분절의 차분이라 그 기울기가 상쇄된다(단 hip 은 가상 몸통이
기준이라 그대로 받는다).

---

## 설정(config)

**한 현장에서 고정인 것을 JSON 하나로 모은다.** 블록은 셋이다.

| 블록 | 무엇 | 왜 고정인가 |
|---|---|---|
| `camera` | 어느 카메라, 노출·게인, ROI, 캘리브레이션 파일 | 현장이 물리적으로 어떤 상태인지 |
| `pose` | 검출(마커 종류와 그 튜닝), 추정(뷰 평면과 그 튜닝) | 재서 알아내는 값, 포즈 정확도에 직결 |
| `server` | 어디서 받는지 | 그 현장의 프로세스가 어떻게 노출되는지 |

**어느 층이든 프로토콜에 노출하지 않는다.** 앞의 둘은 클라가 다른 값을 보낸다고 카메라가 움직이지
않으므로 잘못 계산된 포즈를 받을 자유만 주고, `server` 는 그것을 물어볼 시점에 클라가 이미 접속해
있어 물을 이유가 없다.

반대로 **운영 중 사람이 조작하는 것(rest pose 보정)은 세션 상태**라 config 에 넣지 않는다.

### 직렬화

**저장되는 타입이 자기 키 목록을 스스로 들고 있다.** 구조체 안의 목록 하나가 키를 한 번만 나열해
읽기·쓰기 양쪽을 만든다(필드 추가 = 멤버 바로 아래 한 줄). 목록이 멤버 옆에 있으니 필드만 늘리고
키를 빠뜨리기 어렵다.

그 목록은 포맷을 모른다. JSON 으로 옮기는 엔진과 leaf 변환만 `app_config.cc` 에 있다. 매크로와 컨셉,
그리고 스키마 버전이 어떻게 도장 역할을 하는지는 `src/utils/serializable.hh` 에 있다.

**읽기는 목록이 부르는 키가 하나라도 없으면 거부한다.** 기본값이 빠진 키를 대신하는 것이 이 리더가
막으려는 실패다. 스키마 버전이 먼저 검사되므로 옛 파일은 모양 에러보다 버전 불일치로 멈춘다.

### 색 마커 특유의 제약

- **sagittal 전용이다.** 단색 원반은 거리를 풀 수 없어 frontal 추정기가 쓸 수 없다.
- **어느 다리인지 사람이 알려줘야 한다.** 원반은 자기 쪽을 말하지 못하는데 그 쪽이 슬롯 이름과 각도
  부호를 정한다. 그래서 소스를 열 때 같이 정한다.
- **노출·게인이 auto 면 거부한다.** 색 모델은 a\*b\* 평면의 고정된 값인데 둘 다 휘도를 바꿔 마커를
  거기서 밀어낸다. 단 그 프로파일이 실제로 카메라를 열 때만 검사한다.
- **색 모델과 블롭 필터는 한 블록에 함께 있다.** 같은 자리에서 같은 카메라·거리·조명으로 정해지고,
  점수 문턱은 아예 모델을 기준으로 매긴 값이라 모델이 바뀌면 뜻이 달라진다. 잰 도구가 프로파일을 읽고
  쓰는 그 앱이라 파일을 나눌 이유도 없다.

---

## 디버그 GUI

여기 적는 것은 **불변식**뿐이다. 어느 절에 어느 컨트롤이 있는지는 코드를 본다.

- **config 사본은 하나뿐이다.** 서버가 소유하고 GUI 는 그것을 제자리에서 편집한다. GUI 로 열든
  클라이언트가 START 를 보내든 같은 설정으로 열린다. 예외는 리스닝 포트로, bind 할 때 읽히므로 돌고
  있는 리스너에는 닿지 않는다.
- **컨트롤 패널은 config 프로파일에 담기는지로 갈라져 있다.** 저장 버튼이 "위의 전부"로 읽히는데 그
  위에 안 담기는 것이 섞이면 안 되기 때문이다.
- **패널 편집은 메모리에만 반영된다.** 저장해야 파일에 남고, 소스를 다시 열면 config 값이 다시
  설치된다. 현장에서 GUI 로 맞춘 뒤 저장하면 헤드리스가 그 파일을 읽는 흐름이다. 색 캘리브레이션도
  같은 경로를 탄다.
  - **ROI 만 프로세스 밖에 자국을 남긴다.** 성능이 목적이라 카메라가 실제로 덜 읽어야 뜻이 있고, 그
    설정은 전원을 꺼도 카메라에 남는다(그래서 여는 쪽이 매번 전체로 되돌리고 시작한다). 카메라가 자기
    증분에 맞춰 스냅하므로 **저장은 요청값이 아니라 실제로 걸린 값**을 적는다. 픽셀 프레임이 움직이니
    트래킹 상태가 버려지고, rest 를 픽셀로 들고 있는 sagittal 은 rest pose 까지 잃는다. 녹화를 쓰는
    중에는 거부된다(파일이 프레임 크기를 한 번만 선언한다).
- **프레임 위에서 하는 조작은 창 하나에 모여 있다.** 패널의 프리뷰와 전체화면은 표시만 한다. 마우스도
  캔버스도 하나이므로 **도구는 한 번에 하나만 살아 있고, 연 이유가 곧 그 도구다.** 창을 닫거나 도구가
  쓰는 대상이 사라지면 도구가 내려간다. 패널에는 현재 값 표시와 창을 여는 버튼만 남는다.
- **프로파일을 갈아끼우면 열려 있던 소스를 그 자리에서 다시 연다.** 돌고 있는 파이프라인이 config 와
  어긋난 구간을 만들지 않으려는 것이다. 그 구간을 두면 트래커에 이전 프로파일의 값이 남아 있는데
  저장이 그것을 새 프로파일에 긁어 담는다. 읽기나 검증에 실패하면 현재 설정은 그대로 둔다.
- **로그는 계층 접두사를 단다.** 로그 콘솔의 필터 박스에 접두사를 치면 그 계층만 남는다.

---

## 빌드 (중요)

**반드시 "Visual Studio 2026 개발자 명령 프롬프트(x64)"에서 빌드할 것.** `cl.exe` / `ninja` 가 일반
PATH 에 없으므로 개발자 환경(vcvars64)이 로드된 셸에서만 CMake 구성/빌드가 된다.

```
cmake --preset win64-rel        # 최초 1회 구성 (win64-dbg 도 가능)
cmake --build --preset win64-rel --target exo-skeleton-pose
```

- 툴체인: Ninja + MSVC(x64), vcpkg triplet `x64-windows-static-md`.
- 의존성: OpenCV 는 vcpkg(`deps/vcpkg_installed`) 제공. 그 외는 `cmake/Fetch*.cmake`(SDL3,
  FlatBuffers, uWebSockets 등)와 `deps/`(imgui 계열 정적 라이브러리)로 가져온다. Orbbec K4A Wrapper 는
  prebuilt zip 을 받아 링크(Windows 전용).
- FlatBuffers 바인딩(C++ 헤더 + webview 용 TS)은 **CMake 빌드가 `flatc` 로 자동 생성**한다. 스키마만
  고치면 양쪽이 갱신되므로 별도 수동 생성 단계는 없다.
- 산출물: `out/build/<preset>/exo-skeleton-pose.exe`. K4A 런타임 DLL 과 `extensions/` 는 빌드 후 실행
  파일 옆으로 자동 복사된다.

---

## 실행

단일 실행 파일이 모드로 나뉜다. 서브커맨드가 GUI 냐 헤드리스냐를, `--config` 가 설정을 어디서 읽을지를
정한다.

```
exo-skeleton-pose.exe                                 # 디버그 GUI + 임베드 서버 (기본)
exo-skeleton-pose.exe --config vz-frontal.json        # 그 프로파일로 GUI
exo-skeleton-pose.exe serve --config vz-frontal.json  # 헤드리스 WebSocket 포즈 서버
exo-skeleton-pose.exe serve --config a.json -p 9010 -v # 포트를 갈아끼우고 트레이스까지
exo-skeleton-pose.exe --dump-config                   # 최종 설정을 JSON 으로 찍고 종료
```

**설정을 말하는 인자는 `--config` 하나뿐이다.** 무엇을 어떻게 열지는 전부 프로파일이 정하므로 같은
값을 CLI 로도 말하는 길이 없고, 따라서 둘이 엇갈렸을 때의 우선순위 규칙도 필요 없다. 나머지 인자는
설정이 아니라 **이 실행이 어떻게 노출되고 관찰되는지**를 말하므로 자유롭게 섞인다.

- **`--config` 의 규칙은 한 줄이다: 폴더가 붙어 있으면 그 경로, 아니면 configs 폴더 안의 파일.**
  그 폴더는 저장이 쓰는 폴더와 같아서 읽기와 쓰기가 같은 자리를 가리킨다.
- **설정 파일은 명시적으로 이름을 댔을 때만 읽는다.** 인자 없이 실행하면 파일을 찾아다니지 않고 멤버
  기본값으로 뜬다. 이름을 댔는데 없으면 조용히 기본값으로 흐르지 않고 에러로 멈춘다.
- **`-p` 는 리스닝 포트만 갈아끼운다.** 한 머신에 인스턴스를 둘 띄워 볼 때 쓴다. 무엇을 여는지는 안
  건드리므로 프로파일과 어긋날 여지가 없다.
- **`-v` 는 터미널 로그의 바닥을 내린다.** 프로토콜 트레이스는 debug 이라 헤드리스에서 그것을 보는
  수단이 이 플래그다. GUI 의 로그 콘솔은 자기 싱크로 모든 레벨을 담아두고 자체 토글로 거르므로 이
  플래그와 무관하다.
- webview 데모는 `webview/` 에서 `npm install` 후 `npm run dev` 로 띄운다(먼저 C++ 를 빌드해 TS
  바인딩이 생성돼 있어야 함).

---

## 코드 주석 지침

주석은 **자가완결 최종형**으로 쓴다. 지금의 코드/구조를 있는 그대로 설명하며, **수정·리팩토링 맥락을
담지 않는다.**

- 이번 변경 이력을 모르는 사람이 읽어도 이해에 문제가 없어야 한다. 이력을 알아야만 해석되는 문구 금지:
  "no longer / was X / used to / previously / vestigial / legacy / old approach / instead of the
  previous / now uses ~ (예전 대비)" 등.
- "예전엔 ~였다 / ~를 제거했다" 식 대비·변경 서술 대신, 현재 동작과 그 **설계 근거(무엇을·왜)**만 남긴다.
- **코드에 존재하지 않는 대안을 세워놓고 부정하지 말 것.** "X 대신 Y / X가 아니라 Y / 어느 쪽이든"
  형태는 독자가 X 를 알아야만 성립하는데, X 는 코드에 없다. 채택안을 변론하는 대신 코드가 하는 일과 그
  근거만 서술한다.
  - 나쁨: `// Measured off the printed sheet rather than tuned, so it goes to the pipeline on its own`
  - 좋음: `// Sent through its own setter, which carries it to the detector and the sagittal estimator alike`
  - 나쁨: `// Installed whether the estimator was rebuilt or kept, so an open leaves the config's values in force either way`
  - 좋음: `// _select_estimator leaves an estimator of the same plane standing, so the options are assigned out here`
- **바뀐 결과를 변론하는 문장을 쓰지 말 것.** 지금 코드가 왜 이 모양인지를 독자에게 설득하려 들면, 그
  설득은 바뀌기 전 모양을 전제로만 성립한다. 아래가 전부 같은 실수다.
  - 남긴 것의 존재 이유를 변호하기: "이건 여전히 필요하다 / 아직 쓰인다 / 여기서만 쓴다"
  - 뺀 것의 부재를 해명하기: "여기엔 X 가 없다 / X 는 관여하지 않는다"
  - 고른 것을 다른 후보와 견주기: "셋 중 이것만 ~하다 / 이게 가장 적합하다"
- **다음 표현은 금칙어로 두고 커밋 전에 grep 한다.** 이력을 모르는 독자에게는 뜻이 서지 않는다.
  `no longer`, `instead of`, `rather than`, `used to`, `previously`, `vestigial`, `legacy`,
  `either way`, `not a ~ but a ~`, "더 이상 ~않는다", "이제는 ~한다", "~ 대신".
- 꼭 필요한 곳에만 핵심 위주로 간단명료하게. 자명한 코드에 구구절절 붙이지 않는다.
- **자명한 사실을 길게 쓰는 것도 같은 위반이다.** 분량이 늘어나는 이유는 대개 코드가 아니라 변경을
  설명하고 있기 때문이다. 자명한 부분은 한두 줄로 넘기고, 코드만 봐서는 모르는 것(왜 이 순서인지, 무엇이
  이 값을 보장하는지)에만 줄을 쓴다.
- 출처 표기(예: `Ported from ...`, upstream 커밋 SHA + 파일 경로)는 이력이 아니라 provenance 이므로
  허용한다.
- 문서(`docs/`, `CLAUDE.local.md`)도 같은 원칙. 코드가 바뀌면 드리프트 없는지 함께 점검한다.
